<div class="row">
  
    <%= paginate @users %>
  
  <div class="col-md-offset-4 col-md-8">
    <% if @user.any? %>
      <h3><%= @title %> (<%= @user.count %>)</h3>
      <% @users.each do |i| %>
        <ol  class="users follow">
          <section>
          <%= gravatar_for i %>
          <h3><%=link_to i.name, i %></h3>
          </section>
        </ol>
      <% end %>
    <% end %>
  </div>
  
    <%= paginate @users %>
  
</div>